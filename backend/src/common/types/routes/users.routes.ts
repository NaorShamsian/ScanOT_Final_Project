import { IMessageConstant } from '../log';

export const USERS_ROUTES: IMessageConstant = {
  USERS_PREFIX: 'users',
  FIND_ALL_USERS_PAGINATED: 'all',
  FIND_USER: ':id',
  UPDATE_USER: ':id',
  DELETE_USER: ':id',
};
